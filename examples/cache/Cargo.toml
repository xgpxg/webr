[package]
name = "cache-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
webr = { path = "../..", features = ["cache-memory", "auto-init"] }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
