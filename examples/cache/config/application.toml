[server]
port = 8080
host = "0.0.0.0"

[log]
level = "info"

[cache]
backend = "memory"
default-ttl = 300

[cache.memory]
max-capacity = 10000
time-to-idle = 600

#[cache.redis]
#url = "redis://127.0.0.1"
