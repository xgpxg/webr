[package]
name = "hello-world"
version = "0.1.0"
edition = "2021"

[dependencies]
webr = { path = "../.." }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
